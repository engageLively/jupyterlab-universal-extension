{
  "title": "Galyleo",
  "description": "Settings of the Galyleo extension.",
  "type": "object",
  "jupyter.lab.shortcuts": [],
  "properties": {
    "mode": {
      "type": "string",
      "title": "Mode",
      "description": "If debug, use the debug dashboard; if beta, use the beta  dashboard; if deploy (default) use the deployment dashboard",
      "default": "deploy"
    }
  },
  "additionalProperties": false
}
